language: node_js

# Version number
node_js:
  #- 0.10
  #- 0.12
  #- 4.2.3
  #- iojs-v1
  #- iojs-v2
  #- iojs-v3
  - 5.12.0

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  #pre_ci:
   # - docker build -t=quay.io/shippablequay/shiphitchcock1_sample_node12 .

 # pre_ci_boot:
 #   image_name: drydock/u16nodall
  #  image_tag: master
  #  pull: true
  #  options: '--privileged=true --net=bridge -e FOO=true -e BOO=false'

  ci:
    - node --version  
   # - ps -eax
   # - ls -al 

  post_ci:
    - docker build -t=shippabledocker/sample_node_pvt:officialimgtag .
    - docker build -t=shippabledocker/sample_node:officialimgtag .
    - docker push shippabledocker/sample_node_pvt:latest.40
    - docker push shippabledocker/sample-node:latest
    - docker push shippabledocker/sample_node:officialimgtag
    - docker push shippabledocker/sample_node_pvt:officialimgtag

integrations:
  hub:
    - integrationName: docker
      type: docker
