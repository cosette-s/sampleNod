language: node_js

# Version number
node_js:
  - 0.10
 # - 0.12
  #- 4.2.3
  #- iojs-v1
  #- iojs-v2
  #- iojs-v3
  #- 5.0

env:
  global:
    - FOO=FUBU

  matrix:
    - M1=ONE

build:
  pre_ci:
    - docker build --rm -t=gcr.io/varsha-project/test-aws:$BRANCH.$BUILD_NUMBER .
    #- docker build --rm -t=gcr.io/vidya-project/pyt-img:latest.1 .

  

  ci:
    - ps -eax
    - ls -al

  post_ci:
    
   # - docker build --rm -t=gcr.io/vidya-project/pyt-img:$BRANCH.$BUILD_NUMBER .
    
    - docker push gcr.io/varsha-project/test-aws:$BRANCH.$BUILD_NUMBER

  smoke_test:
    - sudo ls /


integrations:
  hub:
    - integrationName: ship-gcr
      type: gcr    
