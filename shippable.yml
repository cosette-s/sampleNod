language: node_js

# Version number
node_js:
  - 4.2.3

build:

  ci:
    - node --version  
   # - lsb_release -a
    - docker --version
    - docker info
    - docker ps
       
 # post_ci:
   # - docker push shiphitchcock1/samplenod:$BRANCH.$BUILD_NUMBER 

#integrations:
 # hub:
   # - integrationName: shiphitchcock1-hub
   #   type: docker 
    #  branches:
    #    only:
       #   - master           
